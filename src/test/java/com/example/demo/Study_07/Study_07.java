package com.example.demo.Study_07;

public class Study_07 {

    // 📝 解説：コンソールアプリでの CRUD 処理とは？

    // CRUD とは以下の頭文字です：

    // 操作 説明
    // Create 新しいデータを作成する
    // Read データを読み取る（一覧・詳細）
    // Update データを更新する
    // Delete データを削除する

    // Webアプリやデータベースでも必ず出てくる基本的な操作です。
    // コンソールアプリで練習しておくと、仕組みをシンプルに理解できます。

    // 🌱 コンソールアプリの流れ（イメージ）

    // 起動するとメニューを表示する
    // ユーザーが番号を入力して操作を選ぶ
    // それぞれの操作（C/R/U/D）に応じて処理する

    // 1: データ作成
    // 2: データ一覧表示
    // 3: データ更新
    // 4: データ削除
    // 5: 終了
    // 番号を選んでください:

    // 🌱 データ格納方法

    // 最初はデータベースを使わずに、List などのコレクションで管理します。

    // 例：「Book」クラス（id, title, author など）を作ってリストに入れる。

    // 🌱 必要な技術

    // クラス定義（データ用クラス：Bookなど）
    // List操作（追加・検索・削除）
    // Scanner（コンソールからの入力取得）
    // if文 / switch文（ユーザー選択に応じた処理分岐）

    // 📝 練習問題
    // 🔹 問題1：Bookクラスの作成

    // Book クラスを作成してください。

    // フィールド：int id、String title、String author

    // コンストラクタ＋getter/setter＋toString()を作成してください。

    // 🔹 問題2：CRUDメニューの作成

    // Main クラスでコンソールメニューを表示してください。

    // 例：「1:追加 2:一覧 3:更新 4:削除 5:終了」

    // 🔹 問題3：Create処理（追加）

    // List<Book> を使って本のデータを管理します。

    // ユーザーにID・タイトル・著者を入力させ、List に追加してください。

    // 🔹 問題4：Read処理（一覧表示）

    // List に格納された Book をすべて表示する機能を作ってください。

    // 🔹 問題5：Update処理（更新）

    // IDを指定して、タイトルまたは著者を更新できるようにしてください。

    // 🔹 問題6：Delete処理（削除）

    // IDを指定して、List から該当の Book を削除してください。

    // 🔹 問題7：終了処理

    // 5:終了 が選ばれたらプログラムを終了するようにしてください。

    // 📝 補足ポイント

    // Scanner で整数を取得するときと文字列を取得するときの違いに注意

    // Scanner sc = new Scanner(System.in);
    // int id = sc.nextInt(); // 数値入力
    // sc.nextLine(); // 改行読み飛ばし
    // String title = sc.nextLine(); // 文字列入力

    // List は ArrayList を使うのが簡単

    // List<Book> books = new ArrayList<>();

    // 更新や削除では、id で該当する要素を探して操作する

    // 📝 チャレンジ課題（応用）

    // Book に価格や出版日などを追加して複雑にしてみる

    // 並び替え（Comparator＋ラムダ式）を使ってタイトル順に表示する

    // 入力値チェックや例外処理を追加してエラーに強くする

    // 💡 この教材の狙い

    // Javaの基本文法（クラス・メソッド・List操作・Scanner）を組み合わせる力をつける

    // CRUD処理の流れをイメージで掴む

    // 将来的にSpring BootやDBを使ったCRUDに発展させるための基礎になる

    // この練習問題をすべて実装すると、
    // 「簡単なCRUDコンソールアプリを自力で作れる」レベルに到達できます 🎯

}
